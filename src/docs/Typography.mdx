import { Meta, Typeset } from '@storybook/addon-docs/blocks';

<Meta title="Core/Typography"/>

export const typography = {
  type: {
    "sans-primary": "'PT Sans', Helvetica, Arial, sans-serif",
    "serif-primary": "'PT Serif', Georgia, 'Times New Roman', serif",
  },
  weight: {
    regular: '400',
    bold: '700',
  },
  size: {
    xs: 12,
    sm: 14,
    base: 18,
    lg: 24,
    xl: 32,
    xxl: 64,
    xxxl: 96,
  },
};

export const SampleText = 'Lorem ipsum dolor sit amet, consectetur adipiscing elit.';

# Typography

## PT Sans

**Weights:** 400 (Regular), 700 (Bold)

### Regular

<Typeset
  fontSizes={[
    Number(typography.size.xs),
    Number(typography.size.sm),
    Number(typography.size.base),
    Number(typography.size.lg),
    Number(typography.size.xl),
    Number(typography.size.xxl),
    Number(typography.size.xxxl),
  ]}
  fontWeight={typography.weight.regular}
  sampleText={SampleText}
  fontFamily={typography.type["sans-primary"]}
/>

### Bold

<Typeset
  fontSizes={[
    Number(typography.size.xs),
    Number(typography.size.sm),
    Number(typography.size.base),
    Number(typography.size.lg),
    Number(typography.size.xl),
    Number(typography.size.xxl),
    Number(typography.size.xxxl),
  ]}
  fontWeight={typography.weight.bold}
  sampleText={SampleText}
  fontFamily={typography.type["sans-primary"]}
/>

## PT Serif

**Weights:** 400 (Regular), 700 (Bold)

### Regular

<Typeset
  fontSizes={[
    Number(typography.size.xs),
    Number(typography.size.sm),
    Number(typography.size.base),
    Number(typography.size.lg),
    Number(typography.size.xl),
    Number(typography.size.xxl),
    Number(typography.size.xxxl),
  ]}
  fontWeight={typography.weight.regular}
  sampleText={SampleText}
  fontFamily={typography.type["serif-primary"]}
/>

### Bold

<Typeset
  fontSizes={[
    Number(typography.size.xs),
    Number(typography.size.sm),
    Number(typography.size.base),
    Number(typography.size.lg),
    Number(typography.size.xl),
    Number(typography.size.xxl),
    Number(typography.size.xxxl),
  ]}
  fontWeight={typography.weight.bold}
  sampleText={SampleText}
  fontFamily={typography.type["serif-primary"]}
/>
