import { createFooter } from './Footer';

export default {
  title: 'Core/Footer',
  tags: ['autodocs'],
  render: ({}) => {
    return createFooter();
  },
  argTypes: {},
}

export const Footer = {}
