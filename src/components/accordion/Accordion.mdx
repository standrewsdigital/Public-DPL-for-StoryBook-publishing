import { Canvas, Controls, Meta, Source } from '@storybook/addon-docs/blocks';

import * as AccordionStories from './Accordion.stories';

export const componentOnlySource = async (source) => {
  const parsedSource = new DOMParser().parseFromString(source, 'text/html');
  const accordion = parsedSource.querySelector('.accordion-group');

  if (!accordion) {
    return source;
  }

  const prettier = await import('prettier/standalone');
  const prettierPluginHtml = await import('prettier/plugins/html');

  return prettier.format(accordion.outerHTML, {
    parser: 'html',
    plugins: [prettierPluginHtml],
  });
};

<Meta of={AccordionStories} />

# Accordion

The accordion pattern presents an expandable and collapsible section of content. Accordions shorten pages and reduce scrolling, but they require users to click on topic headings to find information. Therefore, accordions should be used sparingly. For more information, see the [Nielsen Norman Group report on accordion usability](https://www.nngroup.com/articles/accordions-complex-content/).

## Rules

- Must not be used when your audience needs most or all of the content on the page to answer their questions. It is better to show all page content at once when the user case supports it. In such cases, do not worry about page length.
- May be used when only one option is relevant to the user, e.g. UK, Europe, USA.
- May be used where you want to step a user through a process, e.g. step 1, step 2, step 3.
- The content within accordions must not contain any headings higher than heading level 4 `<h4>`.
- May be used anywhere in a content section.
- Must only be used on a white or light grey background.
- Content may be divided into columns appropriate to the width of the accordion.

## Single accordion

Consider if the content would be better visible at all times before using a single accordion to hide it from the user.

<Canvas of={AccordionStories.Single} sourceState="none" />

<details>
  <summary>View code options</summary>

### Accordion HTML

Use this when the surrounding page already contains the required layout structure.

<Source
  of={AccordionStories.Single}
  language="html"
  transform={componentOnlySource}
/>

### Accordion with DPL grid

Use this complete example when the container and grid structure are also required.

<Source of={AccordionStories.Single} language="html" />

</details>

<Controls of={AccordionStories.Single} />

## Accordion group

When more than one accordion is grouped together, each accordion should have the same `name` value. For multiple accordion groups on a page, each group must use a different `name` value.

<Canvas of={AccordionStories.Group} sourceState="none" />

<details>
  <summary>View code options</summary>

### Accordion group HTML

Use this when the surrounding page already contains the required layout structure.

<Source
  of={AccordionStories.Group}
  language="html"
  transform={componentOnlySource}
/>

### Accordion group with DPL grid

Use this complete example when the container and grid structure are also required.

<Source of={AccordionStories.Group} language="html" />

</details>

<Controls of={AccordionStories.Group} />