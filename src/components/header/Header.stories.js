import { createHeader } from './Header';

export default {
  title: 'Core/Header',
  tags: ['autodocs'],
  render: ({}) => {
    return createHeader();
  },
  argTypes: {

  }
}

export const Header = {}
